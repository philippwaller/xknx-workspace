[project]
name = "xknx-workspace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["rich>=15.0.0,<16"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
