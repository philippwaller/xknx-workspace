[project]
name = "xknx-workspace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["rich>=14,<15"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
